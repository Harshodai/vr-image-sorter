/* Runtime config placeholder for local development.
   In production (Docker), this file is generated at container startup
   with the actual VITE_API_URL from environment variables.
   During local dev with `npm run dev`, Vite serves this as a static file
   and the app falls through to import.meta.env.VITE_API_URL or the hardcoded fallback. */
